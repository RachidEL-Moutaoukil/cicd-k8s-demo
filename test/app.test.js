console.log('Running simple test');
// Ici vous pourriez avoir des tests réels avec Jest, Mocha, etc.
// Pour ce TP, nous simulons juste que les tests passent
if (true) {
console.log('Tests passed');
process.exit(0);
} else {
    console.log('Tests failed');
    process.exit(1);
}